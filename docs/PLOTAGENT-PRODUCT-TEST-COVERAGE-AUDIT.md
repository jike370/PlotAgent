# PlotAgent 产品测试覆盖审计

> 状态：2026-08-22 发布候选覆盖审计基线。
> 范围：34 个正式图类、Agent/工作流、能力目录、Windows UI、Matplotlib、Origin、持久化与导出。
> 目的：解释既有测试为何没有发现 X38 轴范围缺口，并冻结后续测试分层，避免再用某一层全绿代替产品完整性。

## 1. 结论

既有测试没有“失效”，但覆盖边界被误解了：

- 任务状态矩阵证明规划、追问、确认、执行、部分失败、修复、跳过、重新确认、取消、恢复和重启的状态转换与副作用；它不枚举图类编辑参数。
- SEQ-70 证明冻结样例中的 Agent 决策、对象绑定和恢复质量；编辑样例只覆盖少量标题与轴标题，不是 34 图编辑笛卡尔积。
- Windows 黑盒证明实际执行过的 UI 路径；此前主要覆盖创建和代表性编辑，没有逐图逐参数执行。
- 能力目录、编译器和前端都读取同一份 profile。目录漏掉 X38 `bounds` 时三层会“自洽地拒绝”，只有独立产品期望才能发现。
- 双后端共用 T1 视觉适配器，既有适配器测试证明参数可以执行，但没有证明每张图是否正确声明了该参数。

因此，测试必须同时覆盖“生命周期正确”“能力声明完整”“跨层传递正确”“双后端实际执行”四件不同的事。

## 2. 本轮审计发现与处理

| 发现 | 原因 | 处理 |
|---|---|---|
| Y 偏移堆叠线图不能设置 30–90 的 X 轴范围 | profile 漏掉 `set_axis.bounds`，Agent 和 UI 只能据目录拒绝 | X38 开放 `bounds`，新增目录、工作流编译和 UI 提交回归 |
| 双向误差棒图和误差带共用一整套误差参数 | profile 按“误差”粗粒度开放，UI 也同时提交两类参数 | K06 只开放误差棒四参数；K07 只开放误差带四参数；UI 按参数白名单渲染和提交 |
| 产品文档仍声称支持文本标注 | 旧 T1 设计稿未随产品决策收口 | 正式 profile、图形库和聚焦编辑均不开放，用户能力文档删除该项 |
| 混合图无法精确限制某个轴/系列的参数 | `EngineCapability` 目前只有图类级参数，没有目标级参数 | 记录为合同结构缺口；在引入目标级白名单前，不对 X24 等混合语义图盲目扩大能力 |
| Origin 预检把任意 `Origin64.exe` 硬编码成 2024 SR1 | 只做路径探测，没有读取文件版本 | 预检读取 Windows 文件版本；只接受 10.1.0，错误版本或无版本资源的同名文件在启动前返回 `VERSION_UNSUPPORTED` |

## 3. 当前公开编辑能力

以下数字由 34 个正式 `engine-profile.v1` 统计，表示 Agent、编译器和 UI 可以公开使用的能力，不表示 Origin 的全部菜单能力。

| 能力 | 图数 | 例外原则 |
|---|---:|---|
| 图标题 | 34 | 无 |
| 轴标题与可见性/刻度方向 | 34 | 无 |
| 轴范围 | 30 | K20、K21、S61 为矩阵/类别轴；X24 为混合轴合同缺口 |
| 轴尺度 | 27 | 仅在图类语义允许时开放 |
| 轴反转 | 33 | X24 暂不开放 |
| 系列样式 | 34 | 参数按图元家族不同，不代表每个系列拥有全部参数 |
| 图例 | 29 | 仅有语义图例对象的图类 |
| 数据标签 | 12 | 仅稳定支持的图类 |
| 色板/色标 | 5 | 仅色图类 |
| 误差样式 | 2 | K06 误差棒、K07 误差带 |
| 图类专属参数 | 5 | 由 profile 白名单限定 |
| 独立文本标注 | 0 | 当前产品明确不做 |
| PNG/SVG/OPJU 导出 | 34 | OPJU 仍依赖合格 Origin 环境 |

## 4. 已有测试真正证明什么

| 测试层 | 证明 | 不能替代 |
|---|---|---|
| Pydantic/Schema | 动作字段类型、范围和序列化稳定 | 某图是否应该开放该动作 |
| 任务状态矩阵 | 状态转换、确认授权、幂等、取消和恢复 | 图类、字段、样式与导出正确性 |
| Profile/Compiler | 声明的角色和参数会被接受或拒绝 | 声明本身是否完整 |
| 共享 T1 适配器 | Matplotlib/Origin 能执行公共参数 | 34 个 profile 是否都声明正确 |
| Profile renderer 测试 | 图形结构、数据绑定和若干专属语义 | 完整编辑参数组合 |
| SEQ-70 | 冻结真实模型样例的规划质量 | UI、Origin 菜单和未入样例的能力 |
| Windows UI 黑盒 | 正式入口中的实际可见行为和产物 | 未执行用例、实现内部不变量 |
| 视觉审查 | 默认态或指定编辑态的外观 | 数据绑定、状态机和恢复 |

## 5. 本轮新增的确定性门禁

1. 固定 34 图共同编辑骨架，禁止 profile 静默丢失标题、轴、系列或导出。
2. 固定轴范围、尺度和反转的完整例外集合；新增图类或修改例外必须显式更新产品期望。
3. 固定 K06/K07 的互斥误差参数集合。
4. 固定“有图例对象才有图例动作”。
5. 固定所有正式图类不开放独立文本标注。
6. 固定 X38 自然语言草稿中的 X 轴 `minimum/maximum` 可以通过工作流编译。
7. 固定前端从生成目录获得 X38 轴范围和 K06/K07 误差白名单，并只提交白名单参数。

这些门禁刻意把期望写在独立测试中，不从同一份生成目录反推期望。

## 6. 仍需分层补齐的产品测试

### P0：发布前必须

- 每个正式图类至少执行一次创建、标题、适用轴范围/尺度、一个适用系列样式、图例或明确无图例、撤销、重做和重启恢复。
- 撤销/重做必须是全量逆向：动作含任一无法从当前 PlotDocument 精确恢复的参数时，产品不得生成部分逆动作或显示可撤销状态。当前发布只承诺基础可逆参数；完整历史版本恢复作为后续架构方案保留。
- 每个公开编辑参数至少在 Matplotlib 和 Origin 各有一个真实原生对象读回；OPJU 用全新 Origin 会话重开。
- Windows UI 对每个“公开/不公开”状态各有正反例，不能只检查按钮存在。
- 导入到绘图覆盖 CSV、多工作表 Excel 和带仪器头的 TXT；类型、单位、来源身份和多源对齐均核对。
- PNG、SVG、OPJU 分别验证成功提示、文件存在、内容非空和可编辑/矢量资格。

### P1：稳定性与兼容

- Provider 超时、限流、断网、代理、无额度、坏 JSON、迟到响应和取消。
- Core/Electron 异常退出、项目锁、执行中重启、部分成功恢复和磁盘写入失败。
- 大数据量、长文本、CJK、缺失值、极值、重复类别、动态系列增删和批量任务。
- 打包产物、全新 Windows 用户、无 Origin/错误 Origin 版本和便携路径。

### P2：合同演进

- 为混合图增加“目标级参数白名单”，覆盖 X24 的三根语义轴、X36 的柱/线系列以及 X39/X40 的 connector/column 对象。
- 将需求—profile—Agent 工具—UI 控件—双后端读回—黑盒证据映射成可生成追踪表，评测只允许引用实际证据。

## 7. 发布判定

发布不能只看单一总通过率。至少同时满足：

1. 任务状态矩阵全绿；
2. 独立能力矩阵全绿；
3. 静态、类型、Schema、Python 与前端测试全绿；
4. 正式 Windows UI 冻结黑盒无产品 FAIL，未执行项明确列出；
5. SEQ-70 达到冻结门槛；
6. Origin 产物在新会话中可重开、可编辑且机械读回一致；
7. 工作树干净，源码、生成合同和用户能力文档来自同一提交。

## 8. 2026-08-21 AI 工作台视觉改造定向验证

被测实现节点：`a99a416 feat(desktop): refine AI workbench interface`。

正式入口为 Windows Electron `pnpm dev`，未使用浏览器预览代替桌面证据。定向验证结果：

1. 在隔离项目“新建项目 116”提交“画一张图”，产品生成一条“请先上传数据”Agent 消息；标题、正文和结构化状态没有重复投影，RC-UI-001 关闭。
2. 在历史项目“新建项目 114”复核包含中英文括号的 Agent 消息、任务计划、任务结果和输入区；全角括号可读且未显示缺字方框，RC-UI-002 关闭。
3. 正式桌面实际打开并检查侧栏、Agent 消息、实时运行状态、任务确认卡、任务中心抽屉、已渲染图卡和聚焦编辑面板。
4. 聚焦编辑面板实际打开坐标轴页，轴标题、范围、反转、可见性、刻度方向和应用动作均可发现；本次只做只读冒烟，没有提交用户图形修改。
5. 前端门禁同时通过：29 个 Vitest 文件 / 243 项测试、TypeScript 类型检查、ESLint 和 production build。

这组证据关闭本轮界面已知问题，但不替代第 6 节要求的 34 图完整产品黑盒、Origin 原生读回和 SEQ-70。

## 9. 2026-08-21 版本、导出与编辑状态链统一修复记录

本节保留项目 126 触发的族级审计、施工和关闭记录，不再代表当前开放问题。当前开放问题只在
《PlotAgent V3 发布候选已知问题》中维护，候选证据状态只在第 10 节及机器覆盖账本中维护。

### 9.1 由正式 UI 或持久化证据发现、现已关闭的缺口

| 能力族 | 已确认现象 | 同类影响范围 | 发布前所需证据 |
|---|---|---|---|
| 当前版本与导出 | 项目 126 界面已显示 S34 v3，但三次 OPJU 暂存请求均携带 v1；Origin 因而按旧轴范围执行并失败 | PNG、SVG、OPJU；所有 34 图；连续编辑、Agent 编辑和聚焦编辑 | 导出前以 Core 最新 PlotDocument 为权威同步当前图；三格式请求和结果均记录同一 plot ID/version；不得静默导出旧版本 |
| 导出成功投影 | Core 返回实际 `plot_version`，但前端导出记录不保存或核对它；只要文件 hash/size 存在就会显示成功 | 三格式成功提示、时间线导出记录、黑盒证据 | 成功记录必须包含请求版本和实际版本，二者不一致时不得报告成功；时间线和测试产物都显示版本 |
| 聚焦编辑撤销/重做 | 标题修改成功生成 v4 后，撤销和重做仍禁用 | 标题、轴、图例等编辑面板表单 | 表单只提交实际变更字段，或历史快照覆盖动作中的全部字段；任何可逆基础编辑成功后立即可撤销，撤销后可重做 |
| 历史参数覆盖 | 编辑面板的标题表单总是附带字体、字号、字重、斜体和颜色，而历史记录器只允许 `text`；轴和图例表单也会附带未纳入逆动作的默认高级参数 | 34 图共同编辑面板 | 对所有公开表单逐项比较“提交参数—当前状态—逆动作”；禁止因未修改的默认值使整条历史失效，禁止部分逆向 |
| 重启后的版本权威 | Core 能恢复每图最新 PlotDocument，但尚无一条产品级确定性测试串联创建、连续编辑、撤销、重做、重启和再次导出 | 所有可编辑图类 | 同一 plot ID 的版本单调增加；重启恢复最后提交版本；三格式导出仍使用恢复后的同一版本 |
| 长表 K01 规划 | 正式 UI 中模型选择长转宽并产生 `series_N` 绑定，但 K01 合同要求 `x/y/group`，最终没有形成可执行计划 | 任务规划、数据操作、自动修复 | 冻结长表夹具；错误草稿必须被校验器拒绝并在受控修复后形成合法 K01 绑定，或直接保留长表的 `x/y/group` |
| 多来源确认依据 | 同一任务项包含多个数据来源时，计划卡顶部数据样本只显示第一个来源，无法核对其余来源与绑定 | 多来源同图、批量、多表任务 | 每个计划步骤显示其全部来源身份；每个绑定可追溯到文件/工作表/列；样本预览不得把首来源冒充全部来源 |
| 部分失败 UI 验收 | Core 和前端组件有部分失败/继续未完成项的确定性测试，但正式 UI 没有稳定故障注入入口，无法可靠制造“成功项保留、仅重试失败项” | 部分成功、修复、跳过、恢复 | 提供仅限测试构建或受控夹具的确定性失败点；同一候选上验证不重复成功项、只执行失败项及重启恢复 |

### 9.2 已确认通过、无需重复修补的相邻行为

- 轴范围的非法输入会在前端阻止提交，项目版本不变。
- 切换项目后，旧项目的迟到 Agent 响应不会覆盖当前项目的计划、错误、图形或对话。
- Core 的 `engine.plots.get` 在省略版本时能够返回指定 plot 的最新版本；`engine.exports.execute` 会严格按 `expected_plot_version` 取文档，不会自行把 v1 替换为最新版本。因此版本同步责任必须在导出前显式完成，不能依赖 Origin 或导出后端猜测。

### 9.3 用于关闭上述缺口的确定性回归

1. **当前图版本权威：** 创建 v1，连续编辑到 vN，导出前从 Core 同步同一 plot 的最新版本；界面、PNG、SVG、OPJU 请求与导出结果均为 vN。
2. **三格式一致：** 对同一不可变 PlotDocument，三种格式使用同一 plot ID/version/content hash；旧版本请求不得被当作当前导出成功。
3. **完整编辑状态链：** 创建 → 标题 → 适用轴范围/尺度 → 系列样式 → 适用图例 → 撤销 → 重做；每一步生成单调新版本，逆动作完整而非部分回滚。
4. **重启恢复：** 在撤销/重做后关闭并重新打开项目，恢复最终版本，再次导出三种格式时版本不漂移。
5. **34 图能力覆盖：** 对全部 34 个正式 profile 生成标题、轴、系列动作；对 29 个有图例对象的 profile 增加图例动作；矩阵/类别轴和混合图只执行其合同明确允许的范围/尺度。
6. **表单—历史一致：** 聚焦编辑的标题、轴、系列和图例表单只提交用户实际改变且 profile 允许的字段；所有基础可逆动作产生历史项，高级不可精确逆向动作明确不产生伪撤销。
7. **长表与多来源：** 冻结 K01 长表合法规划/错误修复和多来源计划卡完整来源证据。
8. **部分失败：** 不调用真实模型，使用确定性故障注入证明成功项保留、失败项单独重试、跳过、恢复与重启。

### 9.4 当前统一施工顺序

```text
冻结发布范围与覆盖账本
  → 集中修复已知问题族
  → 全量确定性门禁
  → 正式 Electron 定向验证受影响链路
  → 冻结前端组件、交互和视觉基线
  → 文档一致性、干净工作树与候选提交
  → 候选全门禁及 34 图离线/Origin/编辑/恢复矩阵
  → 数据、多来源、故障、性能和打包矩阵
  → 冻结回归黑盒与独立探索性黑盒
  → 集中修复黑盒问题族并重新冻结候选
  → 只运行一次最终 SEQ-70
  → 发布判定
```

在某一步发现问题时，先回到“问题族修复”，使所有同类对象共享同一修复和回归，再重新冻结；
不得带着已知问题继续后续门禁，也不得为通过验收降低既定产品标准。发布目标编号不得与施工
步骤混用：发布目标 2 是 `34 × 3 × 3 = 306` 的 PNG/SVG/OPJU 逻辑产物矩阵；发布目标 4
是 34 图的标题、轴、系列、图例、撤销、重做和重启恢复矩阵。

### 9.5 功能批次关闭证据

截至本节对应提交，9.1 的功能缺口已按能力族统一关闭，尚未开始前端视觉改造：

1. 导出前先读取 Core 最新 plot 清单；若清单没有当前对象，则必须按当前 ID/version 精确读取并核对。界面版本落后时先同步最新耐久文档，界面超前、对象缺失或回执 ID/version 不一致时一律停止导出。
2. PNG、SVG、OPJU 的成功回执和对话时间线都记录 `plotVersion`。34 图代表态均执行标题、轴、系列和适用图例编辑，PNG/SVG 产物及 OPJU 请求固定为同一最终版本。
3. 聚焦编辑表单只提交实际变化字段；无变化不创建空版本；右侧/底部图例锚点在公开合同值与界面选项间稳定转换。34 个 profile 的基础历史动作均可生成完整撤销和重做。
4. 34 图持久化回归均执行连续公共编辑、撤销标题、重做标题、关闭项目和重新打开，并核对最终版本、父版本和完整 action 顺序。
5. K01 的 `series_N` 错误绑定由 Core 在确认前按图形角色合同拒绝；Pi 同一 activation 必须提交内容不同的修订草稿后才能继续。
6. 多来源计划卡不再只展示“首项示例”，而是逐来源显示文件/工作表身份、字段角色和前三行样本。
7. 正式桌面部分失败测试使用双重显式开关：`PLOTAGENT_ENABLE_UI_TEST_FAULTS=1` 与 `PLOTAGENT_UI_TEST_FAIL_PROFILE_ONCE=<profile>`。故障只触发一次、发生在提交前、默认完全关闭；用于证明成功项保留和失败项单独修复，不得用于发布运行。
8. 编辑派生图时，Agent 上下文从该图最后一次成功的创建/数据更新动作恢复全部不可变来源、
   数据操作与完整字段绑定；不再依赖界面仍选中原始上传表。
9. 编译器为派生字段生成“renderer 角色 → 原始来源字段”证据；确认卡据此逐来源标注角色并
   并行补齐真实样本。读取旧任务计划时先校验原始持久化 JSON，再应用新增合同字段的默认值，
   防止合同升级把未篡改项目误判为哈希损坏。
10. Agent 数据更新与字段重绑进入完整历史链：更新前后的不可变 `data` 和 `bindings` 均保存
    在会话历史项中；撤销先逆转同轮视觉动作再恢复旧绑定，重做先恢复新绑定再重放视觉动作，
    每一步仍通过公开 `bind_fields` 动作产生单调新版本，禁止部分回滚或覆盖旧版本。

候选 `c14619b668cd8745820383b0691f4be03d70a71d` 的确定性门禁：Python `839 passed`；Vitest `29 files / 265 tests`；Ruff、mypy（192 个源文件）、contracts codegen `--check`、TypeScript typecheck、ESLint 和 production build 全部通过。

## 10. 发布覆盖账本

机器事实源为 [《PlotAgent 发布覆盖账本》](./PLOTAGENT-RELEASE-COVERAGE-LEDGER.md) 及其
同名 JSON。生成器 `scripts/build_release_coverage_ledger.py` 从当前 34 图 profile、Origin
recipe 和冻结 release fixture 构建逐图归属，并在门禁中检查：每图必须具备数据合同、
9 个离线 MatrixKey、Origin fresh-reopen、公共编辑、撤销/重做/重启、三格式导出和正式
Windows UI case；任务状态、数据、多来源、UI、故障、导出、性能、打包、黑盒与 SEQ-70
也必须各有可执行归属。本节保留候选运行状态，不再充当手工维护的逐图事实源。

本节把“需求是否真的被测过”与测试总数分开记账。状态只允许
`PASS`、`FAIL`、`BLOCKED`、`UNVERIFIED` 和 `IN_PROGRESS`；只有实际观察到当前候选的
预期结果才能写 `PASS`。单元测试、历史截图、旧提交产物和源码推断不能替代正式 UI、
Origin fresh-reopen 或真实模型证据。

本账本建立时的预冻结代码基线为 `ec4b784587635ea45922eb34ad0429506119d498`；最终候选
必须在发布运行元数据中记录完整 HEAD。任何产品代码变化都会使候选失效，受影响项必须
在新提交上重跑；纯文档账本提交不改变已经执行的产品代码门禁结论。

| 证据编号 | 产品风险 | 当前候选必须覆盖的实际链路 | 当前状态 | 发布证据 |
|---|---|---|---|---|
| RC-DET | 实现内部回归 | Python、任务状态矩阵、能力矩阵、contracts、Ruff、mypy、typecheck、ESLint、Vitest、production build | PASS | 候选 `22e5b13`：Windows 发布脚本内 Python 846、Vitest 282；contracts codegen、Ruff、mypy、typecheck、ESLint、production build 与发布哈希校验全绿；manifest `source_dirty=false` |
| RC-UI-01 | 数据卡不能解释 Agent 实际看到什么 | 正式 Electron 中核对来源身份、整理后字段、类型、单位、真实样本与分页 | PASS | `build/pre-release-exploration/a1a202e/`；正式 Electron 定向链路属于后续透明度能力收缩未影响的 UI 路径 |
| RC-UI-02 | 确认卡无法核对绑定和多来源 | 正式 Electron 中核对每个来源、文件/工作表/列、字段角色、数据操作和确认前无副作用 | PASS | 同上；项目 131 `align_sources_on_x` 与项目 132 `concatenate_sources` 原始证据 |
| RC-UI-03 | 任务状态和错误不可读 | 正式 Electron 中核对计划、运行阶段、部分失败、技术详情、取消与结果投影 | PASS | 同上；定向 6 项合计 `6 PASS / 0 FAIL / 0 BLOCKED / 0 UNVERIFIED` |
| RC-UI-04 | 编辑器与时间线状态漂移 | 正式 Electron 中核对聚焦编辑、tab 保持、结果顺序、基础编辑及数据更新的完整撤销/重做、重启恢复 | PASS | 同上；项目 131 更新后撤销/重做，项目 126 重启恢复 |
| RC-UI-05 | 图形库默认与自然语言图类冲突时错误追问或使用旧图类 | UI 预选只作默认；最新明确自然语言图类直接进入同一闭集计划，并回投 Composer | PASS | 正式打包 Electron 项目 134：Composer/UI 默认 K03，明确请求 K01 后直接生成 K01 计划并回投 K01；证据见 `build/release-ui-targeted-8903c54-20260823-1050/REPORT.md` |
| RC-UI-06 | 批量任务被错误投影成单图，或多来源单图被错误投影成多图 | 同图类批量显示单一图类；异构批量显示“多图任务”；多来源同图显示最终单图类 | PASS | 正式打包 Electron：Events_A/K03 + Events_B/K03 显示 K03；Events_A/K01 + Events_B/K03 显示“多图任务”；项目 109 六来源单图恢复为 X38 |
| RC-UI-07 | 修订、拒绝、执行、重启或迟到响应使 Composer 图类漂移 | 投影跟随最新耐久计划/结果；唯一图类成为续轮默认，异构任务不泄漏隐藏单图；拒绝未执行覆盖时恢复原默认；旧响应不得回写 | PASS | 正式 Electron 已通过拒绝恢复和强制退出后的项目/Composer 恢复；`build/release-ui-revision-22e5b13-20260824/` 证明“修改绑定”保持待确认计划、接受自然语言修订并按新绑定执行；App 89/89 PASS |
| RC-EXP-126 | 界面显示最新图但导出旧版本 | 项目 126 连续编辑后的当前 plot ID/version，经正式 UI 导出 OPJU；回执版本一致，Origin 新会话重开并可编辑 | PASS | `build/release-ui-opju-22e5b13-20260824/`：项目 v12、S34 当前图 v8；正式 UI 重新导出 31,302 B，SHA-256 `3F87E0E46B19515C1C621ED7FEDDC5D609F808AA4BD4C7BC98988DF445237AA1`；OriginPro 2024 新会话打开原生图页与 3 列×61 行工作簿，A1 可编辑并撤销恢复 |
| RC-34-OFFLINE | 图类合同或后端结构遗漏 | 34 图 × minimal/representative/edge × PNG/SVG/OPJU，共 306 个唯一 MatrixKey | PASS | `build/release-matrix/offline-e434e41-20260823-1250/` 为 `272 PASS / 34 Origin 待验证`；由同候选 Origin 阶段关闭为 306/306 |
| RC-34-ORIGIN | OPJU 只是可保存，非原生可编辑 | 34 个 representative 由声明版本 Origin 创建，独立进程重开、机械读回并做可逆编辑 | PASS | `build/release-matrix/origin-e434e41-20260823-1250/`：OriginPro 2024 `10.1.0`，`306 PASS / 0 FAIL / 0 UNVERIFIED` |
| RC-34-EDIT | 公共能力声明与真实图类脱节 | 34 图逐项执行标题、适用轴、系列、适用图例、撤销、重做、重启和三格式同版本导出 | PASS | `build/release-matrix/edits-e434e41-20260823-1250/`：34 profiles、380 汇总项、1889 原子用例，`0 FAIL` |
| RC-DATA | 真实输入不能进入 renderer 合同 | CSV、多 Sheet Excel、仪器 TXT、缺失/极值/长文本、多来源对齐、动态系列增删和大数据导入 | PASS | `operational-e434e41-20260823-1250` 10/10；`data-stress-e434e41-20260823-1250` 8/8 |
| RC-FAULT | 正常路径通过但异常状态失真 | 模型超时/限流/坏 JSON/取消，Core/Origin/磁盘/项目锁故障，部分成功修复、跳过与重启 | PASS | `build/release-matrix/fault-e434e41-20260823-1250/`：13/13 PASS |
| RC-PACK | 源码可运行但发布包损坏或环境门禁失真 | manifest/installer 哈希、打包 Core、真实 K01 渲染与 PNG/SVG、冻结 Origin worker 入口、Origin 缺失/错误/支持版本、隔离 profile Electron 启停 | PASS | `build/release-matrix/packaged-e434e41-20260823-124515/`：扩展后 7/7 PASS；manifest、打包 Core PNG/SVG、冻结 worker 路由、Origin 三种环境状态与隔离 Electron 均通过 |
| RC-PACK-ENV | npm/pnpm 包装环境使发布门禁与直接脚本结果不一致 | `pnpm test:release` 与直接 PowerShell 必须执行同一哈希校验并同为 PASS | PASS | 哈希实现改为显式 .NET SHA-256，不依赖 `Get-FileHash` 模块自动加载；候选发布脚本中的 package 入口与独立直接 PowerShell 入口均 PASS |
| RC-VIS | 机械正确但默认视觉退化 | 当前候选 34 图视觉签名由人工逐图审查 | IN_PROGRESS | `build/visual-audit/release-signatures-c14619b-20260823-030405/` 已生成，等待人工结论 |
| RC-BB | 组件分别正确但真实长链路失败 | 同一冻结候选的完整 Windows Electron 黑盒；覆盖 34 图代表路径、Agent、多数据任务、编辑、导出和恢复 | UNVERIFIED | 待黑盒报告、CSV、截图与产物 |
| RC-SEQ70 | 真实模型语义与对象绑定不稳定 | 同一冻结候选的一次完整 SEQ-70；不得用重复运行挑选最好结果 | UNVERIFIED | 待 REPORT.md/report.json |
| RC-REL | 用局部全绿代替发布结论 | 汇总以上证据和 known issues；关键范围零 FAIL、零 BLOCKED、零应测 UNVERIFIED | UNVERIFIED | 待最终发布判定 |

### 10.1 34 图逐项索引

34 图的唯一逐项索引由生成账本维护。每行同时给出 9 个离线 MatrixKey、Origin
fresh-reopen case、公共编辑 case 前缀、撤销/重做/重启 case、三格式导出 case 和正式
Windows UI case。未产生任一必要证据时，该图不能记为发布通过。手写图名表已删除，避免
与 profile、Origin recipe 或 release fixture 漂移。

### 10.2 防遗漏规则

1. 发布汇总必须从本账本生成，不允许只报告“总测试通过数”。
2. 同一能力在不同层分别记账：确定性测试不能替代 UI，UI 不能替代 Origin 读回，SEQ-70 不能替代状态矩阵。
3. 探索性黑盒必须包含测试计划之外的自由操作；发现新问题后先审计同类能力族，再决定修复范围。
4. 任何 `UNVERIFIED` 都必须说明是尚未执行、证据不足还是环境阻断；环境阻断应改记 `BLOCKED`，不能继续保留模糊状态。
5. 修复使 HEAD 变化后，先按《实施计划》的最小回归原则判定哪些旧证据失效，再更新本账本。

## 11. 2026-08-24 冻结 UI 缺口的能力族修复

旧候选的正式桌面覆盖已完成 34/34 图类归属与视觉检查；随后在多来源、部分失败和连续编辑的长链路中发现公共层缺口。本轮不逐图打补丁，也不重跑未受 renderer/profile 影响的 34 图视觉矩阵，而是冻结以下跨层不变量：

| 风险族 | 产品不变量 | 确定性证据 | 正式 UI 状态 |
|---|---|---|---|
| 来源选择 | UI、Main、Core 与 workflow 合同均允许超过 8 个显式来源；当前公开上限 32 | 九来源 App/Main/Core/workflow/inspection 合同测试；33 来源稳定拒绝 | PASS；`22e5b13` 正式 UI 9/32、九项计划、九项执行，项目 `v9 → v18` |
| 部分失败 | 语义失败停在 `partial` 等待用户选择；成功项不重跑；只有无副作用的确定性失败可直接安全重试 | Agent runtime 的稳定 partial、无自动 pump、用户修订与接受成功项测试 | PASS；`4dcc23b` 正式故障夹具完成失败项定向重试且成功项未重跑 |
| 非有限数值 | 缺失值和 NaN/±Inf 语义分离；`is_finite`/`is_not_finite` 可由 Agent 调用，诊断上下文保持 JSON 安全 | 数据操作语义、合同 codegen、Pi host JSON 边界与 Agent 提示测试 | PASS；正式 UI 计划显示 `y is finite` 并成功执行 |
| 图类专属参数 | profile 已公开的 `set_chart_parameter` 必须能从自然语言进入草稿；不得只在 renderer 存在 | K21 `triangle=lower/upper/full` profile、backend 与 Agent 提示回归 | PASS；正式 UI “只保留下三角”计划及结果 |
| 编辑读回 | 同一图例对象的分次动作合并读回；版本变化必须重新挂载对应预览资源 | productState 合并读回、编辑→撤销→重做的 v2/v3/v4 预览断言 | PASS；正式 UI 图例分次编辑、标题编辑及撤销/重做 `v8 → v13` |
| 规划停止 | 停止先 abort provider，再读取串行 Core 检查点 | planning pump 调用顺序测试 | PASS；正式 UI 在规划中停止并显示项目未发生更改 |
| 时间线与 Composer | 计划位置不移动，结果在其下方追加；唯一确认图类或“多图任务”回投 Composer | App 时间线顺序、@图目标、单图/多图投影、拒绝与重启恢复测试 | PASS；上述批量、部分失败和连续编辑正式链路冒烟 |
| 发布宿主 | 发布入口无论由 Windows PowerShell 还是 `pwsh` 启动，都使用可验证存在的系统 Windows PowerShell 执行离线签名/哈希校验 | 两种宿主下的 release-tools 测试与 `release-windows.ps1 -DryRun` | PASS；`22e5b13` Windows 发布脚本生成 installer 并返回 `UNSIGNED_DEVELOPMENT_VERIFIED` |

本节的发布顺序固定为：全量确定性门禁 → 形成干净候选提交 → 打包 Windows Electron → 只复测上表受影响链路 → 更新本账本与 known issues → 运行唯一一次 SEQ-70。任何定向 UI 产品 FAIL 都会使候选失效并返回能力族审计，不允许边测边逐点修补。
