# PlotAgent 施工、测试与发布路线

> 状态：当前唯一交付顺序。本文规定从需求进入施工直到发布判定的先后关系；产品范围见 [PRD](./PRD.md)，测试覆盖见 [产品测试覆盖审计](./PLOTAGENT-PRODUCT-TEST-COVERAGE-AUDIT.md)，发布门槛见 [Beta 性能测试与发布门禁](./PERFORMANCE-TEST-RELEASE.md)。

## 1. 固定顺序

```text
冻结发布范围与覆盖账本
  → 集中修复已知问题族
  → 全量确定性门禁
  → 正式 Electron 定向验证受影响链路
  → 冻结前端组件、交互和视觉基线
  → 文档一致性、干净工作树与候选提交
  → 候选全门禁及 34 图离线/Origin/编辑/恢复矩阵
  → 数据、多来源、故障、性能和打包矩阵
  → 冻结回归黑盒与独立探索性黑盒
  → 集中修复黑盒问题族并重新冻结候选
  → 只运行一次最终 SEQ-70
  → 发布判定
```

正式 Electron/Origin 定向验证已完成；六项固定用例在 `a1a202e` 上为 6 PASS / 0 FAIL / 0 BLOCKED / 0 UNVERIFIED，证据位于 `build/pre-release-exploration/a1a202e/`。首轮公共编辑矩阵随后发现 12 个混合图类的 Origin 透明度语义不独立，产生 64 条动作级联失败；该首轮候选没有被误记为通过。

透明度缺口已按能力族处理：OriginPro 2024 只能为同一原生混合绘图对象稳定持久化一个透明度值，混合线点、填充与边缘不再宣称可分别设置两个不同透明度；纯线、纯点、纯填充，以及位于不同原生对象上的透明度仍保留。上述数字是历史候选的记录，不代表当前候选。

问题发现基线为 `781e793`。该提交的 34 图、Origin fresh-reopen、公共编辑、数据/故障/运行/打包矩阵及复合编辑历史已经形成证据；统一 UI 发现阶段也已结束。唯一确定的任务能力 FAIL 是“用户确认语义后，非同构宽表/长表仍不能形成同构合并结果”，同时保留数据处理确认可读性、长规划反馈和来源数量文案三个公共 UX 缺口。冻结问题及关闭标准以 [发布候选问题台账](./PLOTAGENT-V3-RELEASE-CANDIDATE-KNOWN-ISSUES.md) 为准。

当前处于第 5 阶段“正式 Electron 定向验证”的一次公共入口返修。`ef40c96` 首轮结果为 10 PASS / 1 FAIL / 0 BLOCKED / 1 UNVERIFIED：多来源宽/长表整理、确认预览、K03 创建、导出、Origin 原生重开和重启恢复均通过；已有图编辑在模型调用前以 `PI_V2_RUNTIME_FAILED / invalid request parameters` 失败，Stop 因没有可停止的独立运行态而未验证。证据位于 `build/stage5-electron-ef40c96-20260825-004501/`。

根因是已有 prepared 图上下文的授权校验漏认 `declare_unit` 输出字段；编译器能执行该字段，但编辑激活上下文把相同绑定误判为未授权。修复必须统一全部数据操作的输入/输出字段血缘，不能给 K03 或标题编辑加例外。修复后的发布快线固定为：停止冻结前重复生成全量 34 图矩阵 → 运行字段血缘、已有图上下文、Main/Pi 契约与编辑状态机聚焦门禁 → 正式 Electron 只复测“已有图编辑计划并执行”和“Stop 后项目/图版本不变” → 两项通过即冻结候选 → 只在候选上各运行一次完整 34 图矩阵与冻结黑盒 → 0 FAIL、0 BLOCKED、必测 0 UNVERIFIED 后仅运行一次 SEQ-70。冻结前不再重复执行候选级重矩阵。

`eb99ab2` 的上述两项正式 Electron 复测均通过：编辑任务形成确认卡并使项目 `v3→v4`、图 `v1→v2`，独立 Stop 任务为 `cancelled` 且项目 v4/图 v2 不变；证据位于 `build/stage5-edit-stop-eb99ab2-20260825-034336/`。随后产品负责人将“Origin 每个原生图层默认显示四侧完整轴框”确认为本版发布阻断项，因此 `eb99ab2` 候选及其后续 Origin/编辑证据失效。该变更必须位于所有 34 个 Origin renderer 之后的共享原生后处理层，先统一四侧默认态，再由显式 `set_axis(axis_line_visible=...)` 覆盖目标轴线；不得逐图修改模板或影响 Matplotlib 默认态。新候选须重跑 Origin 34 图 fresh-reopen、视觉签名和公共编辑矩阵。

`c6ca622` 随后完成冻结黑盒 52 项，结果为 47 PASS / 5 FAIL / 0 BLOCKED / 0 UNVERIFIED；该候选的发布判定为 FAIL。五项失败不是五个独立补丁，而是四个公共合同族：复数系列作用域被缩成单系列、批量计划只投影最后一个结果、Agent 来源授权在 32 项被截断且“授权来源/实际采用来源”语义混淆、多来源单图因重复调查未及时形成终态，以及混合导入结果埋在数据卡内部。产品负责人确认接受改版与完整重跑成本，因此 `c6ca622` 的黑盒结论只保留为问题证据，不作为新候选 PASS 继承。

本轮统一改版的冻结标准为：`set_series_style` 具备明确的单目标/全部系列作用域并由实际 backend readback 展开；一个计划的全部输出按稳定 `@图N` 顺序追加到时间线；UI、Main、TaskEnvelope、AgentContext、Workflow 与多来源数据操作统一支持最多 64 个授权来源，任务项只声明实际采用的子集；已具备来源、字段与图类事实的多来源任务在同一 activation 提交计划而不重复调查；每次混合导入产生独立、逐文件可读的结果消息。完成确定性门禁和定向 UI 后重新冻结并完整重跑黑盒，只有 0 FAIL / 0 BLOCKED / 应测 0 UNVERIFIED 才运行最终 SEQ-70。

`c87e264` 的第二轮冻结黑盒完整执行 52 项，结果为 50 PASS / 2 FAIL / 0 BLOCKED / 0 UNVERIFIED；34 个支持图类全部通过。两个失败继续按合同族集中处理：一是 `align_sources_on_x` 的第二个数值输出曾可被误绑为 K01 的 `group`，形成数值分组而非两条真实系列；二是混合导入 UI 用例的系统文件选择器实际只向应用返回两个文件，未把第三个 ragged 文件纳入 `selected_files`。前者通过编译器不变量、Agent 初次规划/返修指引及“对齐→宽转长→x/y/group”真实数据流测试统一关闭；后者已由 Main IPC 级三文件用例证明应用会逐一调用 Core 并返回两个成功、一个具名拒绝，下一轮 UI 必须在只含三个冻结输入的独立目录中全选，并以结果回执中的三个文件名证明输入确实成立。不得把文件选择未成立误记为产品处理失败，也不得用单文件补测替代混合结果。

下一候选仍完整重跑 52 项，不继承 `c87e264` 的 50 项 PASS。Origin 四侧完整轴框已经位于所有 renderer 共用的原生后处理层，下一轮 OPJU fresh-reopen 证据必须同时确认四侧轴线；任一产品 FAIL 都继续阻断最终 SEQ-70。

`2568d01` 的 52 项冻结黑盒全部实际执行并取得 52 项用例 PASS，但额外 Origin 视觉读回发现一项发布阻断缺口：Matplotlib 中“全部系列改为红色”已生效，fresh-open OPJU 仍显示一黑一红。请求和机械读回均包含两条红色设置，根因是 Origin 官方模板的分组递增样式仍覆盖单条 plot 命令。该轮因此整体判定为 FAIL，不因用例表全绿而发布。统一修复位于 Origin 共享系列适配器：多 plot 图层发生公开系列编辑前执行表现解组，再按语义系列应用动作，保留原生图与来源绑定；默认四侧轴框不变。真实 K01 OPJU 已确认两条系列均为红色且四侧轴框仍完整，聚焦 Python `121 passed`、Main/Pi `82 passed`，正式 Electron 已有图标题编辑使项目 `v40→v41`、图 `v5→v6`，独立 Stop 后仍为项目 v41 / 图 v6。上述仅允许冻结新候选，仍须在新提交上重跑完整候选门禁与 52 项黑盒。

### 1.1 冻结范围与覆盖账本

- 先更新 PRD、产品决策、能力目录和验收口径，再动实现。
- 明确本轮新增、修改、删除和不做的能力；禁止一边施工一边扩大范围。
- 变更图类、字段合同、公开编辑参数或导出语义时，先更新独立产品期望，不从实现反推需求。
- 实现前必须把需求展开成完整的任务状态转换矩阵、能力矩阵和跨层追踪表；未冻结矩阵时不得用成功路径切片代替范围定义。

### 1.2 集中修复问题族

- 按一条可交付纵切实现：合同 → Core → Main/Preload → Renderer → 双后端/持久化。
- 每个切片同步补 Schema、类型、正反例、错误语义和受影响文档。
- 暴露一个缺口时先审计其所属能力族和全部消费者，再统一修复；禁止只修当前图类、当前参数或当前状态。
- 可见交互必须接真实状态；不得只补前端外观，也不得用补丁绕过领域合同。

### 1.3 全量确定性门禁

- 运行任务状态矩阵、能力矩阵、Python、TypeScript、Schema/codegen、lint、typecheck 和生产构建。
- 测试同时覆盖生命周期、能力声明、跨层传递和双后端执行，不能由同一份目录自证完整。
- Core 状态、权限、幂等、副作用和错误恢复使用确定性测试；不得调用真实模型替代合同测试。
- 公共能力按“全部正式图类 × 适用参数 × 双后端 × 保存/重开”生成或枚举测试，不能从少量代表图外推。
- 已通过全量门禁后，若定向 UI 只暴露不改变 renderer/profile 的公共合同缺口，冻结前允许先运行精确聚焦门禁；完整全门禁与 34 图重矩阵移到新候选且只运行一次。聚焦门禁不能替代候选门禁，只用于避免在候选冻结前后重复生成同一重矩阵。

### 1.4 正式 Electron 定向验证

- 先确认 Core、Electron、provider 配置和必要的 Origin 环境可启动；环境冒烟只排除噪声，不代替功能测试。
- 对本轮修改路径、历史高风险路径和错误恢复做正式 Electron 实测。
- 保存截图、对象 ID/版本、导出物和重启前后身份。
- 定向测试通过只允许进入冻结，不等于完整产品已经验收。

本轮授权后的首个定向批次固定覆盖：

1. 选中已有派生图，仅以 `@图号` 下达修改；Agent 计划必须恢复该图全部不可变来源、既有数据操作和完整绑定，不依赖界面仍选中原上传表。
2. `align_sources_on_x` 与 `concatenate_sources` 各取一例；确认卡逐来源显示文件/工作表/列、原始字段角色和真实样本，不显示内部 field ID 代替用户身份。
3. 确认数据更新后核对结果位于确认卡下方，并执行撤销和重做；旧/新完整 `data`、`bindings` 与 plot version 单调性必须一致。
4. 打开含旧合同 TaskPlan 的历史项目；原始持久化 JSON 哈希应保持有效，不得因新增默认字段被误判损坏。
5. 从 Core 最新 plot ID/version 导出 PNG、SVG、OPJU；回执与时间线版本一致，OPJU 在独立 Origin 会话中打开并可编辑。
6. 重启项目后恢复最后提交版本；不要求恢复仅属于上一桌面会话的 redo 栈。

### 1.5 冻结前端组件、交互和视觉基线

- 确认信息层级、时间线顺序、组件边界、状态归属和可访问语义；组件只消费结构化产品状态。
- 功能结构稳定后统一排版、颜色、密度、动效和响应式细节。
- 涉及图形默认态或 renderer 的变化，先生成当前提交的视觉产物，由人工审查后才能冻结。
- 纯组件样式变化无需重跑 34 图 renderer；影响画布、导出或 Origin 的变化必须重跑受影响图类。

### 1.6 文档一致性、干净工作树与候选提交

- 删除重复、过期或与当前合同冲突的说明；所有当前事实以覆盖账本和机器生成索引为准。
- `git diff --check`、生成物一致性和工作树归属必须核对，现有用户改动不得被覆盖。
- 在合适节点提交，固定产品代码、依赖、fixture、模型配置、Origin 版本和后续输出目录。

### 1.7 候选全门禁与 34 图矩阵

- 在候选提交上重跑确定性门禁。
- 执行 34 图 minimal/representative/edge × PNG/SVG/OPJU 离线矩阵。
- 执行 Origin fresh-reopen、公共编辑、数据更新、撤销/重做、重启恢复和三格式同版本导出矩阵。
- 逻辑产物、原生可编辑性、机械读回和人工视觉证据分别记账，不互相替代。

### 1.8 数据、多来源、故障、性能和打包矩阵

- 覆盖 CSV、多 Sheet Excel、仪器 TXT、缺失/极值/长文本、多来源对齐、动态系列增删和大数据导入。
- 覆盖模型、Core、Origin、磁盘与项目锁故障，以及部分成功、修复、跳过、取消和重启。
- 核对响应、导入/渲染性能、安装包、首次启动和升级/拒绝路径。

### 1.9 冻结回归与探索性黑盒

- 只从正式 Windows Electron 入口执行，不以源码、单测、静态页面或历史产物补证。
- 覆盖 34 图代表路径、Agent 任务、数据导入/处理、编辑、撤销重做、恢复、重启和三种导出。
- `PASS` 必须实际观察到预期；未执行为 `UNVERIFIED`，环境阻断为 `BLOCKED`，产品错误为 `FAIL`。
- 冻结回归按已知合同执行；独立探索性黑盒只根据产品功能说明自行设计路径，二者证据分开保存。

### 1.10 集中修复黑盒问题族并重新冻结

- 黑盒暴露问题后立即停止沿用旧候选结论，先审计同类能力族和跨层消费者。
- 修复后重跑受影响门禁、定向 UI 和矩阵，以新提交重新冻结；不得发现一个补一个后继续旧候选。

### 1.11 最终 SEQ-70

- 在同一个冻结候选上从零运行真实模型评测，验证规划、对象/字段绑定、追问、工具、修复和恢复。
- SEQ-70 不替代 UI、renderer、Origin 或导出黑盒；它只证明冻结任务集上的 Agent 资格。
- 最终候选只完整运行一次，不重复抽取最好结果。

### 1.12 发布判定

- 汇总确定性门禁、视觉签名、Windows 黑盒、Origin fresh-reopen、SEQ-70 和 known issues。
- 关键范围必须为零 `FAIL`、零 `BLOCKED`、零应测 `UNVERIFIED`。
- 不得为了发布降低合同、忽略问题、沿用陈旧证据或把“已记录”写成“已通过”。

### 1.13 本轮施工教训形成的长期约束

本节不是复盘备注，而是后续每轮都必须遵守的执行约束：

1. **矩阵先于实现。** 先冻结状态、能力、后端、持久化和用户可见结果的完整交叉关系，再开始施工。
2. **同类缺口一次审完。** 单点失败只是审计入口；修复范围由能力族决定，不由当前失败样例决定。
3. **确定性与模型评测分层。** Core 状态、权限、幂等、副作用和错误恢复只由确定性测试证明；真实模型只证明语义理解和规划质量。
4. **冻结后再验收。** 黑盒和 SEQ-70 只针对同一干净候选；任何产品代码变化都会使受影响证据失效。
5. **不同证据不互相代替。** 单元测试、机械读回、视觉审查、正式 UI、真实模型评测和用户验收分别记账。
6. **不以发布为由降质。** 发现系统性问题时允许延长施工，但不允许忽略、缩窄合同或用补丁式特例换取表面全绿。

## 2. 变更后的最小回归原则

| 变更 | 必须重跑 |
| --- | --- |
| 纯文案或非结构样式 | 组件测试、可访问性检查、定向 UI |
| 时间线、确认卡、编辑器组件 | 前端全量、对应 Core 投影、定向 UI |
| Agent 合同或运行时 | 状态矩阵、Agent 全量、定向 UI、SEQ-70 |
| Profile/公共编辑能力 | 能力矩阵、编译器、前端目录、双后端、受影响 UI |
| 单图 renderer/Origin recipe | 该图 default/edited/dynamic/fresh、视觉审查、导出 |
| 共享 renderer/T1 适配器 | 所有受影响图类；无法精确界定时重跑 34 图 |
| 导入、存储或项目 schema | 导入 golden、迁移/拒绝、重启恢复、打包与 UI |
| 发布脚本或依赖 | 完整构建、安装包校验和发布门禁 |

## 3. 当前产品基线

- 34 张正式单图；不支持组合图或自由画布。
- Pi 是可替换 Agent 运行循环；Core 是任务、权限、确认、版本和执行权威。
- Matplotlib 与 Origin 使用独立 renderer；Origin 产物必须是原生可编辑 OPJU。
- 用户可从图形库明确选图，也可在自然语言中明确指定正式图类；存在实质歧义时 Agent 追问。
- 正式桌面当前不公开 WorkflowRecipe 保存/重放，也不公开独立文本标注。
- 项目 schema 当前为 v7；本机 v5/v6 工作区只允许代码已覆盖的事务性增量迁移，其他版本稳定拒绝。

## 4. 当前前端施工决策记录

本节记录为避免阻塞而由施工过程直接作出的取舍，最终统一视觉审计时必须向用户复述，不得把未采用方案隐藏为“没有考虑”。

| 决策 | 当前选择 | 未采用方案 | 取舍原因 |
| --- | --- | --- | --- |
| AI 界面参照 | 采用 Beautiful UI 的 Loading/Thinking、Approval、Task Rows、Chat、Prompt Bar、Diff/Table、Sidebar、Fine-tune Inspector 模式 | 复制参考站主题或组件源码 | 只需要成熟信息层级；PlotAgent 仍需自己的领域状态、Windows 桌面语义和科研数据对象 |
| 组件依赖 | 继续使用现有 React、Lucide 与 CSS token；复杂无障碍 primitive 出现实际需要时再单组件引入 | 整包引入 Tailwind/shadcn、assistant-ui 或另一套 design-system runtime | 全量迁移会扩大桌面包、样式和测试风险，不直接增加绘图能力 |
| 视觉方向 | 冷灰蓝工作台、深墨色主操作、纯白图表画布、单一蓝色焦点 | 默认灰白黑、深色/霓虹 AI 主题、绿色品牌底色 | 提高产品辨识度，同时让图形而非产品 chrome 保持视觉主角 |
| 组件边界 | 统一 Agent 消息原语；对话、确认、任务状态与结果继续消费现有结构化状态 | 重写时间线、引入第二套前端领域状态 | 现有时间线合同已通过确定性测试，视觉施工不应重做任务编排 |
| 测试范围 | 纯 UI 变化先跑前端全量与正式 Electron 定向测试 | 重跑全部 34 图 renderer/Origin | 当前变更不修改画布、PlotSpec、renderer 或导出语义；若后续触及这些范围再扩大回归 |
