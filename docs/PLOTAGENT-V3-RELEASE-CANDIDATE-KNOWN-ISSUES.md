# PlotAgent 发布候选已知问题

> 本文件只保留当前候选仍未关闭的问题。已经完成能力族修复但等待正式桌面证据的事项列为“待定向复测”，不提前记为 PASS；历史问题从 Git 与《PlotAgent 产品测试覆盖审计》追溯。

## 当前实现问题

本轮集中审计后，暂未保留“已知但不修”的实现缺口。旧候选暴露的同源问题已按能力族处理；
以下五项刚完成实现、尚待新包正式 UI 关闭，不能提前记为 PASS：

- `WORKFLOW_NON_ISOMORPHIC` 返修必须逐项对齐字段名、逻辑/物理类型和单位；若单位或字段语义缺少证据，Agent 应精确追问，不能重复必然失败的合并方案。
- 一次自动返修仍失败且零成功项时，task/item 进入 `failed`；任务中心不再显示“进行中”或“停止任务”，对话明确说明返修耗尽且项目未修改。
- 复合编辑的撤销/重做必须在新候选正式 UI 中证明；实现已改为来源/结果 PlotDocument 快照恢复，不再依赖前端猜测模板默认值。
- 所有 `WORKFLOW_*` 数据/结构错误必须交回 Agent 修订或追问；确定性技术故障的同参数重试最多一次。
- 日期文本只有在显式格式和 `ordinal_day` 模式下才能转换为 numeric；X38 的 X 绑定不再接受 text/datetime 直接进入 renderer。

其余已处理问题：

- 多来源选择不再在第 8 个来源处截断；当前单次任务最多显式选择 32 个来源，合同、桌面上下文和 Composer 使用同一上限。
- 语义部分失败按证据充分性分流：完整、无副作用且不缺用户事实的计划结构错误由 Agent 自动修订并重新确认；缺少用户事实时才追问；旧失败计划不再原样重试。
- 数值列可显式筛选有限值与非有限值；NaN、正负无穷不会再使 Agent 上下文在 JSON 边界崩溃。
- 图例编辑状态由同一图例对象的历史动作合并读回，不再因分次设置可见性和位置而显示旧默认值。
- 撤销、重做和连续编辑后的预览按 plot ID、版本与资源 ID 重新挂载，防止界面保留旧位图。
- Agent 对 profile 已公开的图类专属参数不得静默遗漏；K21 三角矩阵方向作为代表用例固定为 `lower`、`upper`、`full`。
- 规划中停止先中止当前模型流，再读取串行 Core 检查点，避免停止操作排在长模型调用之后。
- 发布校验显式解析系统 Windows PowerShell，不再假设当前 `pwsh` 的 `$PSHOME` 中存在 `powershell.exe`。
- Agent 编辑在确认前保存精确 plot/version 快照，`@图N` 指定目标后的成功修改进入同一撤销/重做历史。
- Core 进程恢复到 ready 后自动重新激活当前项目并读回数据表与最新图版本，不要求用户手动切换项目。
- Matplotlib 在视觉动作应用完毕后统一校正含 CJK 文本的字体，避免 profile 局部 Latin 字体污染后续中文标题。

## 定向复测结果

本轮能力族定向复测已关闭，结果为 `8 PASS / 0 FAIL / 0 BLOCKED / 0 UNVERIFIED`：

1. 九个同构来源在 `22e5b13` 正式打包 Electron 中保持 `9/32`，形成并执行 9 个独立 K01 任务项；项目版本 `v9 → v18`。证据：`build/release-ui-targeted-22e5b13-20260824/`。
2. 批量任务一项失败后保持 `partial`，成功项保留，任务中心同时给出适用的“仅重试失败项”和“保留成功项并结束”；定向重试后完成。证据：`build/release-ui-targeted-4dcc23b-20260824/evidence/04-*`。
3. 含 NaN/正负无穷的数据由 Agent 使用 `is_finite` 形成计划并成功渲染，不再出现原始 JSON `inf` 报错。证据：同目录 `02-*`。
4. K21 “只保留下三角”形成并执行 `triangle=lower`。证据：同目录 `04-k21-*`。
5. 分次编辑图例、标题后读回当前值，撤销、重做时预览与版本同步变化。证据：同目录 `05-*`。
6. 规划中停止及时生效；相关正式链路中的时间线保持追加，Composer 保持当前任务投影。证据：同目录 `03-*` 及上述批量/编辑链路。
7. 项目 126 的 S34 当前图 v8 在 `22e5b13` 正式包中重新导出 OPJU；UI 回执、磁盘大小和 SHA-256 一致。OriginPro 2024 全新打开原生图页和 3 列×61 行工作簿，单元格可编辑且撤销恢复。证据：`build/release-ui-opju-22e5b13-20260824/`。
8. “修改绑定”保持原待确认计划，自然语言将 S34 的 `-Z''` 从 `Z'' (b)` 修订为 `Mag`，重新确认后按新绑定生成 `@图5 · v1`，项目 `v12 → v13`。证据：`build/release-ui-revision-22e5b13-20260824/`。

34 个图类的冻结默认视觉、Origin 原生创建/重开和公共编辑矩阵已在旧候选完成，不因上述公共运行时/UI 修复重复执行。定向复测只覆盖受影响链路；若发现 renderer 或 profile 变化，再使相应视觉证据失效。

## 最终发布证据缺口

- `4469072` 的全量确定性门禁与打包通过，中文标题、`@图N` 编辑撤销/重做、Core 自动恢复和首轮计划结构自动返修已通过正式 UI；返修耗尽后的任务中心终态失败，因此该候选作废。
- 当前修复的 Python `853/853`、Ruff、mypy、contracts codegen、TypeScript、ESLint、Vitest `286/286`、production build 与 Windows 打包工具测试已通过；快照历史修复后需重新提交、重新打包，并在正式 Windows UI 中复测复合编辑撤销/重做、工作流失败分流、日期数值整理和重启恢复。
- 定向复测通过后，仍需按发布账本完成同一冻结候选的完整黑盒和唯一一次最终 SEQ-70。
- SEQ-70 完成前不作发布通过结论，也不得通过重复运行挑选较好结果。
