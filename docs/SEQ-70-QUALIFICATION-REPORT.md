# PlotAgent v3 SEQ-70 Pi Agent 资格报告

日期：2026-08-13
最终判定：**GO**

## 1. 冻结范围

- 源提交：`74d0e1f4b1b2eca43ad37272772c018ab66512df`
- 运行时：生产 `PiAgentRuntime` + 隔离 desktop Core
- Provider：`custom.default`
- 模型：`deepseek-v4-flash`
- 冻结任务：24 项，每项重复 3 次，共 72 次
- 模型任务：18×3 = 54 次
- 运行时任务：6×3 = 18 次
- Task set SHA-256：`6ab81cc1188a6ec1f0b788c7ba787e5772952b96b2b2340eefab83e0aace5398`
- 正式产物：`D:\plotv3\build\seq70-agent-eval\20260813-pi-isolated-74d0e1f`

评测使用合成数据和独立项目。每个模型任务×重复轮次使用独立项目、数据和计划命名空间，不读取或修改用户项目。密钥仅由 Core 从 Windows 凭据存储取得，未写入报告或评测目录。

## 2. 最终结果

| 指标 | 结果 | 门槛 | 判定 |
|---|---:|---:|---|
| 候选计划合法率 | 100% | ≥95% | PASS |
| 本地校验接受率 | 100% | ≥95% | PASS |
| 对象绑定准确率 | 100% | ≥95% | PASS |
| 错误自动绑定率 | 0% | 0% | PASS |
| 字段映射首轮正确率 | 100% | ≥90% | PASS |
| 必要追问率 | 100% | ≥90% | PASS |
| 无效追问率 | 0% | ≤5% | PASS |
| 模型任务精确成功率 | 100% | ≥90% | PASS |
| 运行时任务成功率 | 100% | 100% | PASS |
| 模型延迟 P95 | 7.7753 秒 | ≤30 秒 | PASS |

计数：`72 PASS / 0 FAIL`。

## 3. 覆盖内容

模型任务覆盖：

- 6 类创建与字段映射：折线、散点、双向误差棒、分组柱状、热图、前后对比；
- 图标题、X/Y 轴标题与比例、固定范围、系列样式、图例、PNG 导出；
- 无修改请求；
- 未指定图类的本地必要追问；
- 已指定图类但缺字段时的必要追问；
- 原始单元格直接编辑越界拒绝；
- 不支持图类禁止静默替换。

运行时任务覆盖：

- 确定性 Core 预检不调用模型；
- Provider 未配置时 fail-closed；
- 模型未调用决策工具时拒绝；
- 同轮多次提交决策时拒绝；
- Core 对决策保持最终权威；
- 新请求取代旧请求后，旧请求不得写入计划或覆盖状态。

## 4. 延迟与成本

- 模型延迟中位数：4.1620 秒
- 模型延迟 P95：7.7753 秒
- 模型延迟最大值：8.2229 秒
- 输入 token：723,448（cache read 674,048 / cache miss 49,400）
- 输出 token：28,611
- 按冻结价格估算成本：¥0.120103

## 5. 评测期间发现并关闭的问题

1. Pi 工具参数原先直接使用判别联合 Schema，DeepSeek function calling 要求顶层 `type: object`。现改为 `{ decision: <EngineAgentDecision> }` 包装，Core 仍校验内部完整决策。
2. 外部 Pi JSON 原先用严格 Python 对象模式校验，JSON list 会被误判为非 tuple。现按 canonical JSON 合同校验，不放松业务 Schema。
3. Pi Core 返回结构化拒绝时，运行时原先仍可能显示 completed。现所有 `accepted=false` 都进入 failed，不生成成功状态。
4. 评测器最初跨重复轮次复用同一项目，稳定 plan/action ID 会造成评测自身的状态冲突。正式运行改为每个任务×轮次独立项目，冻结任务和门槛不变。

## 6. 结论边界

本报告确认的是 Pi Agent 决策与本地权威绑定资格，不代替桌面 UI 黑盒、34 图视觉验收或 Origin OPJU 可编辑性验收。四图 renderer 复核另见 `docs/VISUAL-REREVIEW-K06-X13-X38-X40.md`。
