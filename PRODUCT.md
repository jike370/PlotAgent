# Product

## Register

product

## Users

面向跨学科的通用科研用户，包括研究生、实验人员和研究团队。他们通常已有 CSV、XLSX 或实验仪器导出的表格数据，希望在不了解复杂绘图软件操作的情况下，快速获得可信、可继续编辑的科研图表。

## Product Purpose

产品以对话驱动科研绘图。用户在本地项目中导入一份或多份数值数据，通过图形库或自然语言明确指定图形类型，由 Agent 完成单图、同类批量、不同数据对应不同图类的批量任务，以及受控的多数据同图绘制，并支持持续对话修改。结果可以导出为 PNG、SVG 和原生可编辑的 Origin `.opju` 文件。第一轮聚焦数值数据绘图，不导入或处理科研图像。首版成功的核心标准是让用户愿意尝试，并尽快从真实数据得到可信、可复现的科研图表。

## Brand Personality

严谨、清晰、有探索感。表达克制而友好，像一位熟悉科研规范的协作伙伴，不故作高深，也不过度承诺。

## Anti-references

不采用传统后台管理系统的卡片堆叠，不把复杂参数面板作为默认入口，不采用过度科幻的深色界面，也不把产品做成只有输入框和文字回复、缺少数据与图形上下文的通用聊天机器人。

## Design Principles

1. 对话就是工作流：上传、理解、绘图、修改和导出都在连续任务上下文中完成；缺少数据、图形或字段时仍可提交自然语言，由 Agent 在对话中引导补齐；同一模型请求串行执行。
2. 尽快展示可信结果：让用户在首次任务中用最少操作看到基于真实数据生成的图。
3. 用户明确指定图形：既可在图形库选择，也可在自然语言任务中逐项点名；Agent 不主动推荐、猜测或替换未被用户指定的图形类型。
4. 科研细节可追溯：导入结构、字段映射、受控绘图准备、固定计算、图形版本、任务阶段和导出结果始终可查看；当前对可逆编辑提供撤销/重做，并比较当前与直接父版本。
5. 复杂度逐步展开：默认保持简洁，需要时再进入坐标轴、图层和样式等精细编辑；v1 不内置通用科学分析或拟合引擎。

全部已确认产品细节记录在 [docs/PRODUCT-DECISIONS.md](docs/PRODUCT-DECISIONS.md)，可实施范围记录在 [docs/PRD.md](docs/PRD.md)。任何后续范围变化都应同时更新两份文件。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为基线。正文和控件满足对比度要求；图表默认使用色盲友好配色，并避免仅依靠颜色表达差异；完整支持键盘操作、清晰焦点状态和语义化标签；所有非必要动画尊重 `prefers-reduced-motion`。
