# fig-agent 产品说明

## 产品定位

fig-agent 是一款面向科研数据的 Windows 本地绘图 Agent。用户可以直接导入原始表格数据，用自然语言说明单图或批量绘图任务，并继续提出标题、坐标轴、系列和样式等修改要求。产品使用同一份受控绘图状态生成 Matplotlib 预览、PNG、SVG 和 Origin 原生可编辑的 `.opju` 文件。

fig-agent 的目标是减少整理数据结构、逐项填写绘图参数和重复修改 Origin 图形的操作，同时保留字段绑定、数据处理、图形版本和导出结果等可以核对的过程。产品只负责把用户明确提出的绘图目标转换为可执行任务，不替用户解释科研结论。

## 适用用户

产品面向已经拥有实验或统计数据的研究生、实验人员和科研团队。当前输入以数值表格为主，支持 CSV、TSV、TXT、DAT、XLS、XLSX 和 XLSM，不导入或处理科研图像。

## 主要能力

### 直接导入原始数据

- 一次可以导入一个或多个文件，Excel 的多个工作表分别呈现。
- 用户不需要先把数据整理成 fig-agent 的固定模板。程序先识别表头、字段类型、单位候选、数据区域和来源；只有结构存在多个同等合理的解释时才提出必要问题。
- 原始文件保持只读。筛选、排序、字段选择、严格类型/结构化标量提取、宽长转换、同构拼接和单位处理等受控操作会形成带字段血缘的新数据视图，不会覆盖原始数据；结构化映射提取还可以生成“键是否存在”的独立布尔字段，从而区分可选键缺失和整个源值缺失。未知结构、未声明键和不匹配类型会明确失败，不静默清洗。

### 用自然语言绘图和改图

- 当前图形库包含 34 种正式单图。用户可以先在图形库选择图类，也可以在自然语言中明确指定。
- 支持单图、同类批量、不同来源对应不同图类的批量任务，以及受支持的多来源同图任务。
- Agent 可以根据用户要求整理数据、声明字段绑定并生成待确认计划。用户确认后才执行绘图。
- 后续可以继续通过自然语言修改受支持的标题、坐标轴、系列、图例和样式。可逆修改会形成图形版本，并支持撤销和重做。
- K04 颜色映射气泡图支持把布尔或离散字段通过显式、穷尽的“值—形状”表映射为逐点符号；未声明值、未使用声明值和不受支持形状都会明确失败，不由后端猜测默认形状。

### 导出 Origin 原生图

- PNG 和 SVG 由本地 Matplotlib 后端生成，不依赖 Origin。
- OPJU 由本机 Origin 创建，包含原生工作表、图页、图层、绘图对象和实际使用的数据绑定，可以继续在 Origin 中编辑。
- 自然语言修改和界面修改进入同一图形版本；再次导出 OPJU 时，受支持的改动会写入 Origin 原生对象。产品不会反向导入或直接修改任意现有 OPJU。

## 当前运行条件

- 当前公开测试版为 `fig-agent 0.1.1`，测试基线是 Windows 11 x64。
- 自然语言任务需要用户配置可用的模型服务。产品提供智谱开放平台、DeepSeek、阿里云百炼和自定义兼容服务的配置入口；不配置模型时仍可使用本地导入、图形库和不依赖自然语言的结构化操作。
- 安装包已经包含 fig-agent 所需的 Python 运行环境和 Origin 连接组件，用户不需要另行安装 Python、配置环境变量或安装 `originpro` 包。
- 用户可以在侧栏手动选择本机 Origin 主程序。选择结果会保存在本机，并用于后续检测和 OPJU 导出。

## Origin 兼容范围

- 当前程序接受文件版本为 `10.1.0` 的 Origin 2024，并已在 OriginPro 2024 SR1（`10.1.0.178`）完成完整验证。
- OriginPro 2024 SR0 和标准版 Origin 2024 不会被版本校验拦截，但尚未完成与 SR1 相同范围的完整验证。
- Origin 2024b、2025、2025b、2026 及其他文件版本目前不受支持。程序会显示检测到的真实版本，不会把版本不兼容误报为未安装。

## 数据和模型服务

项目文件、图形版本和导出结果保存在本机。使用自然语言任务时，用户指令、字段元数据和受控数据样本会发送到用户选择的模型服务；数据保留规则由对应服务决定。API Key 保存在 Windows Credential Manager 中，不写入项目文件。

## 当前范围边界

- 产品只提供 34 种受支持的单图，不提供组合图、多面板自由编排或任意 Origin 对象编辑。
- 产品不内置通用科学分析、拟合、统计检验、任意 Python、SQL 或 Shell 执行。
- Agent 不主动猜测、推荐或替换用户没有明确指定的图类；存在真实歧义时会继续询问。
- OPJU 导出依赖本机已安装并获得授权的受支持 Origin；PNG 和 SVG 不受这一条件影响。

## 品牌与内部名称

所有用户可见界面、安装包、官网和对外说明统一使用 `fig-agent`，官网域名为 `fig-agent.cn`。内部 Python 包、RPC 协议、存储标识和历史工程文档继续使用 `plotagent` 或 `PlotAgent v3`，避免品牌调整影响已有项目和工程兼容性。这些内部名称不作为对外产品名。

## 界面和表达

产品的表达保持严谨、清晰和克制。界面以项目、数据、任务、图形和导出结果为主要对象，不把复杂参数面板作为默认入口，也不把产品简化成缺少数据与图形上下文的通用聊天窗口。交互以 WCAG 2.2 AA 为基线，关键操作应当支持键盘访问，状态不能只依靠颜色表达，非必要动画应当尊重 `prefers-reduced-motion`。

## 产品原则

1. 对话就是工作流。导入、任务说明、确认、绘图、修改和导出都保留在项目时间线中。
2. 用户掌握图类和最终执行。Agent 可以检查数据和提出计划，但不能在用户确认前修改项目或生成正式结果。
3. 原始数据保持不变。所有数据处理、字段绑定、图形版本和导出结果都可以追溯。
4. 默认界面保持简洁。复杂参数只在用户需要继续修改时展开。
5. 产品说明只陈述已经实现或已经明确验证的能力，不把内部合同、测试工具或未来计划写成当前功能。

权威产品边界记录在 [产品决策](docs/PRODUCT-DECISIONS.md)，可实施需求记录在 [产品需求文档](docs/PRD.md)。范围或兼容声明发生变化时，应当同时更新这两份文件。
