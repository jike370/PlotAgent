export const PUBLIC_PRODUCT_NAME = 'fig-agent'
export const PUBLIC_AGENT_INITIALS = 'FA'

// Keep the established storage location stable while the public brand changes.
// This is an internal compatibility identifier, not user-facing copy.
export const LEGACY_USER_DATA_DIRECTORY_NAME = 'PlotAgent'
